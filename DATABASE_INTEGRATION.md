# Database Integration Guide

Этот документ описывает интеграцию приложения HanJaemi с базой данных Supabase.

## 🗄️ Структура базы данных

### Основные таблицы

1. **user_profiles** - Профили пользователей
2. **user_subscriptions** - Подписки и планы пользователей
3. **user_daily_usage** - Ежедневное использование
4. **user_monthly_usage** - Ежемесячное использование
5. **user_learning_progress** - Прогресс обучения
6. **user_settings** - Настройки пользователей
7. **user_achievements** - Достижения пользователей
8. **user_chat_history** - История чатов

### Функции базы данных

- `can_user_make_request(user_uuid)` - Проверяет лимиты использования
- `increment_user_usage(user_uuid, request_type)` - Увеличивает счетчик использования
- `update_updated_at_column()` - Обновляет поле updated_at

## 🔧 Настройка

### 1. Переменные окружения

Убедитесь, что у вас настроены следующие переменные:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### 2. Создание схемы базы данных

Выполните SQL скрипт из файла `supabase-schema.sql` в вашем Supabase проекте:

```bash
# В Supabase Dashboard -> SQL Editor
# Скопируйте и выполните содержимое supabase-schema.sql
```

## 🚀 Использование

### UserService

Основной сервис для работы с пользовательскими данными:

```typescript
import { userService } from '@/lib/services/userService';

// Создание профиля пользователя
const profile = await userService.createUserProfile({
  email: 'user@example.com',
  full_name: 'John Doe',
  preferred_language: 'en'
});

// Проверка лимитов использования
const limits = await userService.checkUsageLimits();

// Увеличение счетчика использования
await userService.incrementUsage('chat');

// Получение прогресса обучения
const progress = await userService.getUserProgress();
```

### API Endpoints

#### Пользовательские данные
- `GET /api/users/profile` - Получить профиль
- `PUT /api/users/profile` - Обновить профиль
- `GET /api/users/usage` - Получить лимиты использования
- `POST /api/users/usage` - Увеличить использование
- `GET /api/users/progress` - Получить прогресс
- `PUT /api/users/progress` - Обновить прогресс
- `GET /api/users/settings` - Получить настройки
- `PUT /api/users/settings` - Обновить настройки

#### Интеграция с существующими API
- `/api/chat` - Автоматически отслеживает использование
- `/api/evaluate-writing` - Автоматически отслеживает использование

### React компоненты

#### UsageLimits
Отображает текущие лимиты использования:

```tsx
import { UsageLimits } from '@/components/usage-limits';

<UsageLimits />
```

#### LearningProgress
Отображает прогресс обучения:

```tsx
import { LearningProgress } from '@/components/learning-progress';

<LearningProgress />
```

#### UserSettingsComponent
Управление настройками пользователя:

```tsx
import { UserSettingsComponent } from '@/components/user-settings';

<UserSettingsComponent />
```

## 🔄 Автоматическая инициализация

При первом входе пользователя система автоматически:

1. Создает профиль пользователя
2. Создает подписку по умолчанию (free план)
3. Создает настройки по умолчанию
4. Инициализирует прогресс обучения

Это происходит в `app/auth/callback/route.ts`.

## 📊 Отслеживание использования

Система автоматически отслеживает:

- Количество запросов к API
- Тип запросов (chat, grammar, vocabulary)
- Ежедневные и ежемесячные лимиты
- Историю чатов

## 🧪 Тестирование

Запустите тест интеграции с базой данных:

```bash
node scripts/test-db-integration.js
```

## 🔒 Безопасность

- Все таблицы защищены Row Level Security (RLS)
- Пользователи могут видеть только свои данные
- API endpoints проверяют аутентификацию
- Использование лимитов проверяется перед каждым запросом

## 📈 Мониторинг

Для мониторинга использования можно использовать:

1. Supabase Dashboard - для просмотра данных
2. API endpoints - для программного доступа
3. React компоненты - для отображения в UI

## 🚨 Обработка ошибок

Система обрабатывает следующие ошибки:

- Превышение лимитов использования (429)
- Отсутствие аутентификации (401)
- Ошибки базы данных (500)
- Неверные данные (400)

## 🔧 Обслуживание

### Очистка старых данных

```sql
-- Удалить старые записи использования (старше 1 года)
DELETE FROM user_daily_usage 
WHERE usage_date < CURRENT_DATE - INTERVAL '1 year';

DELETE FROM user_monthly_usage 
WHERE usage_month < DATE_TRUNC('month', CURRENT_DATE - INTERVAL '1 year');
```

### Обновление лимитов

```sql
-- Обновить лимиты для всех пользователей
UPDATE user_subscriptions 
SET max_requests_per_day = 20, max_requests_per_month = 600 
WHERE plan_type = 'free';
```
